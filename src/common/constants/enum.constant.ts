export enum roles {
    SUPER_ADMIN = 'super_admin',
    ADMIN = 'admin',
    USER = 'user'
}

export enum genders {
    MALE = 'male',
    FEMALE = 'female',
}

export enum systemRoles {
    SUPER_ADMIN = 'super_admin',
    ADMIN = 'admin',
    USER = 'user'
}

export enum TokenTypes {
    ACCESS = 'ACCESS',
    REFRESH = 'REFRESH'
}

export enum notificationTypes {
    GENERAL = 'general',
    ORDER = 'order',
    SYSTEM = 'system',
}
